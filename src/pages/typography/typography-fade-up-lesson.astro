---
import "../../styles/styles.scss";
import CodeViewer from "../../components/CodeViewer.astro";

const html = `
<section>
  <div class="container">
    <h1><span>Split Text</span></h1>
    <h1><span>Fade Up</span></h1>
    <h1><span>Smooth Out</span></h1>
  </div>
</section>
`;
const css = `
  .container {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    height: 100vh;
    color: white;
    background-color: var(--theme-black);
    font-family: "Grotesk-Overused", sans-serif;
    transform: translate(-50%, -50%);
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
  }

  h1 {
    font-size: 10rem;
    font-weight: 900;
    width: 100%;
    text-transform: uppercase;
    letter-spacing: -0.05em;
    padding: 0;
    margin: -0.125rem 0;
    text-align: center;
    line-height: 0.85;
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
    will-change: transform;
    color: white;
  }

  h1 span {
    display: block;
    position: relative;
    will-change: transform;
    transform: translateY(100%);
  }
`;
const javascript = `
<script is:inline src="/scripts/gsap.min.js"></script>
<script is:inline src="/scripts/CustomEase.min.js"></script>
<script>
  const headerSpans = document.querySelectorAll("h1 span");
  gsap.registerPlugin(CustomEase);
  const customEase = CustomEase.create("custom", ".87,0,.13,1");
  const tl = gsap.timeline();

  tl.to(headerSpans, {
    y: "0%",
    duration: 1,
    ease: "power3.out",
    stagger: 0.05,
    delay: 0.5,
  }).to(headerSpans, {
    y: "-100%",
    ease: "power3.out",
    stagger: 0.09,
    delay: 2,
  });
</script>
`;
---

<style lang="scss">
  .lesson {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1rem;
    width: 55vw;
    margin: 0 auto;
    padding-top: 5rem;
    height: 100%;
    min-height: 100%;

    &__header {
      display: flex;
      flex-direction: column;
    }

    &__header h1 {
      font-size: 2rem;
      font-weight: 500;
      color: white;
    }

    &__header span {
      display:flex;
      flex-direction: column;

      &:first-child {
        font-weight: 200;
      }
    }

    &__header a {
        color: white;
        text-decoration: none;
    }    

    &__content {
        min-height: inherit;
        p {
            font-size: 1.5rem;
        }
    }
  }
</style>

<section class="lesson">
  <header class="lesson__header">
    <h1>
      <span>Lesson</span>
      <span>Translate Up</span>
    </h1>
    <a href="/typography/typography-fade-up">DEMO</a>
  </header>
  <section class="lesson__content">
    <p>
      The translate up animation is a simple animation that moves the text up
      from the bottom of the screen.
    </p>
  </section>
  <section class="lesson__code">
    <CodeViewer html={html} css={css} javascript={javascript} />
  </section>
</section>


